/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["encrypted-tbn0.gstatic.com","media.designcafe.com","images.unsplash.com"]
    }
};

export default nextConfig;
